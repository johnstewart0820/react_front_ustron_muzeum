import React, { useState, useEffect } from "react";
import { withRouter } from "react-router-dom";

const Tourism = (props) => {
    const {history} = props;

    return (
        <>
            Tourism
        </>
    );
}

export default withRouter(Tourism);
