import React, { useState, useEffect } from "react";
import { withRouter } from "react-router-dom";

const ChronicleMuseumEvents = (props) => {
    const {history} = props;
    return (
        <>
        Chronicle_museum_events
        </>
    );
}

export default withRouter(ChronicleMuseumEvents);
