import React, { useState, useEffect } from "react";
import { withRouter } from "react-router-dom";

const Chronicle = (props) => {
    const {history} = props;

    return (
        <>
        </>
    );
}

export default withRouter(Chronicle);
