import React, { useState, useEffect } from "react";
import { withRouter } from "react-router-dom";

const EducationIndustriada = (props) => {
    const {history} = props;

    return (
        <>
        EducationIndustriada
        </>
    );
}

export default withRouter(EducationIndustriada);
